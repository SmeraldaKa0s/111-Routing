import { createContext } from "react";

// por definicion siempre vamos a tener un proveedor --> dice que info vamos a tener
// componente consumidor --> lea info que le pasa el proveedor
const UserContext = createContext();


export default UserContext;